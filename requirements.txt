# Core server/runtime
flask
flask-socketio
flask-cors
eventlet
python-dotenv
setuptools<81
numpy
scipy

# Audio capture / streaming
sounddevice
pyaudio
webrtcvad

# Denoising / audio IO
pyrnnoise
rnnoise-cli
soundfile>=0.12.0
librosa>=0.10.0

# Offline STT (Whisper)
openai-whisper>=20231117
torch>=2.0.0

# TTS (PyKokoro / MeloTTS)
transformers>=4.30.0
pykokoro>=0.6.5
spacy>=3.7.0
pyopenjtalk
cn2an>=0.5.17
jieba>=0.42.1
ordered_set
pypinyin
nltk>=3.8

# ONNX runtime (PyKokoro backend)
# - Windows/Linux: prefer GPU build
# - macOS: CPU/CoreML-compatible fallback
onnxruntime-gpu; platform_system != "Darwin"
onnxruntime; platform_system == "Darwin"
